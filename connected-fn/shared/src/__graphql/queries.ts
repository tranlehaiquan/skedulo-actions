declare module "*/fetchJobs.graphql" {
  import { DocumentNode } from "graphql";
  const defaultDocument: DocumentNode;
  const fetchJobs: DocumentNode;

  export { fetchJobs };

  export default defaultDocument;
}
