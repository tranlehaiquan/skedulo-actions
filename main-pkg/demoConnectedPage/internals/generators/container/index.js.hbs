/**
 *
 * {{properCase name }}
 *
 */

{{#if memo}}
import React, { memo } from 'react';
{{else}}
import React from 'react';
{{/if}}
interface IProps {
  title: string
}

const {{ properCase name }}: React.FC<IProps> = ({ title }) => {
  return (
    <div>
      {title}
    </div>
  );
}

{{#if memo}}
export default memo({{ properCase name }})
{{else}}
export default {{ properCase name }}
{{/if}}